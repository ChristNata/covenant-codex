# Re-review (adversarial lens): codex-fork after cycle-2 + cycle-3

The plan claims to have closed the prior adversarial blockers (S1 stale-G4
false-green, S2 F14 same-root, S3 env-scrub case aliases, S4 F00 completeness,
S5 two-tool viability) and to have removed code signing (OQ4). Try to prove the
fixes are cosmetic or that the signing removal opened a new hole. Overwrite
`docs/master-plans/cross/codex-fork/review-adversarial.md` in the `review.md`
format. Rigor: hard. Phase: plan. Reviewers: adversarial. Tag shippability
blockers `blocker`.

## Read

- `docs/master-plans/cross/codex-fork/master.md`, `review.md`,
  `repo-context/**`, `schema/decide-v1.json`
- `docs/master-plans/cross/child-runtime-native/master.md` S2, C3, C6, C7, G4, P6

## Attack

1. **S1 fix real?** Do the new F12/F13 discriminating tests + the
   `g4_schema_id`/`g4_semantics_id` sidecar pin + F33 SC6 / F32 SC4 actually
   make it impossible to promote/adopt against a G4 that ignores the extra
   fields? Or can the attested id be self-asserted by the same stale sidecar
   (i.e., an id string that isn't cryptographically bound to real field
   evaluation)? Find the gap.
2. **S2 fix real?** Does F32 SC5's real-adapter separation test actually prove
   distinct roots at the integrated launch, given CRN C3 today sets both homes
   to one dir and this plan cannot edit C3? Can F32 pass while CRN ships the
   same-root arm?
3. **S3 fix real?** Is the "one immutable Windows env block, case-insensitive
   keys, reject collisions, closed secret set" now airtight, or is there still a
   spelling/encoding/duplicate path where a control var or provider secret
   reaches a descendant while SC7 stays green?
4. **S4/S5 fixes real?** Does `AUTHORITY-INVENTORY.toml` + F31 SC8 genuinely
   force one negative mutation per sink family (including new upstream families),
   or can a new authority still evade it? Does F11 SC8's fixture read-turn prove
   viability, or can it pass while a real reviewer/planner workload still needs a
   denied tool?
5. **Signing removal — new holes?** With signing gone, is SHA-256 + F31 +
   provenance a sufficient integrity basis for a binary the harness downloads
   and runs? Can a release asset be swapped for a different exe whose
   `codex.exe.sha256` and inventory were regenerated by the same pipeline, such
   that F32 pins attacker bytes? Is that risk acceptable for a single-user local
   tool, or did removing signing break a criterion that still references it
   (dangling F33/F32/provenance/SC)? Check F33 ordering is still coherent
   (build → hash → inventory → provenance → publish) with nothing assuming
   post-sign bytes.
6. Any NEW contradiction introduced by either pass (a criterion that now
   references a dropped field/step, a Produces/dependency mismatch, a doc that
   disagrees with the plan).

Refuted-by-default; raise only substantiated findings with concrete
inputs/state → wrong outcome. Do not edit files.
